# Monopin 公開準備

## 今できていること
- 部屋ごとの名前・質問・参加URL・QRコード
- 主催者だけの質問変更、ピンのリセット、受付終了・再開
- 学生・研修参加者はピンの位置だけを回答
- 学外公開用の静的サイト構成（GitHub Pages）
- Firebase Authentication と Realtime Database の接続処理・アクセスルール
- 公開前の自動テストとGitHub Actionsの公開手順

## 接続のために必要なもの
1. 公開先GitHubリポジトリのURL
2. Firebaseプロジェクト
3. FirebaseのWebアプリ設定（firebaseConfig の内容）

firebaseConfig はWebサイトに埋め込む接続設定です。
Googleアカウントのパスワード、秘密鍵、サービスアカウントJSONは貼り付けないでください。

## Firebase側の設定
1. プロジェクトにWebアプリを登録する。
2. Realtime Database を作成し、databaseURLを含むWeb設定を取得する。
3. AuthenticationでGoogleログインと匿名ログインを有効にする。
4. Authenticationの承認済みドメインにGitHub Pagesのホスト名を追加する。
   例：ユーザー名.github.io（URLのパス部分は含めません）。
5. firebase/database.rules.json をRealtime Databaseのルールとして反映する。
   テストモードの誰でも読み書き可能なルールでは公開しない。
6. 公開前に rules の自動テストを実行する。

主催者だけがGoogleログインを使います。部屋の所有者は認証されたユーザーIDで決まります。
参加者は匿名認証を自動で行うため、名前・メールアドレス・パスワードを入力しません。
部屋のURLを知る参加者は質問・受付状態と自分のピンを読み書きでき、主催者は全員のピンを読めます。
他の部屋への主催者操作・他人のピンの書き換え・終了中の回答をルールで拒否します。

## GitHub側の設定
1. リポジトリの Settings → Pages → Source を GitHub Actions にする。
2. Settings → Secrets and variables → Actions → Variables に
   MONOPIN_FIREBASE_CONFIG を追加し、Web設定をJSON形式で入れる。
   必須項目：apiKey / authDomain / projectId / databaseURL / appId。
3. 標準のリポジトリURLなら、公開パスは自動判定される。
   独自のパスが必要な場合だけ MONOPIN_BASE を設定する（例：/monopin/）。
4. mainに更新を送るか、Publish Monopin ワークフローを手動実行する。
5. テストと生成が成功した後に、GitHub Pagesへ公開される。

公開用ビルド：npm run build:pages
出力先：dist-web
公開用URLは /?view=join&room=... 形式なので、GitHub Pagesのサブフォルダーでも直接開けます。
app/api のPC用サーバー処理は、公開用ファイルには含まれません。

## 検証状況と残り
PC用の権限テスト：npm run test:sessions
Firebaseの権限テスト：npm run test:firebase（Java 21とネット接続が必要）
GitHubの自動チェックでは、Java 21を準備してFirebaseの権限テストも実行します。

検証ツールの準備は完了しましたが、このPCではJavaのローカル通信エラーでFirebaseエミュレーターが起動しませんでした。Firebase権限テストは未完了です。GitHub ActionsのLinux環境でテストを通過することを公開条件にしています。
公開先設定、Googleログイン、実際のFirebaseでの保存、学外からの接続はまだ未確認です。
公開用ファイルを作れることと、実際に全国から利用できることは別の確認項目です。
公開後は携帯回線と、離れた場所の数名で確認します。
想定人数での同時接続と利用上限・費用の確認を終えてから本番研修に使用してください。

## 参考
- Firebase 匿名認証：https://firebase.google.com/docs/auth/web/anonymous-auth
- Firebase セキュリティルール：https://firebase.google.com/docs/database/security/rules-conditions
- GitHub Pages ワークフロー：https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages

